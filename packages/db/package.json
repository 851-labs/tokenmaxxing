{
  "name": "@tokenmaxxing/db",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun run typecheck",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "test": "vp -C ../.. test run --project db",
    "typecheck": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:",
    "@types/bun": "catalog:",
    "better-sqlite3": "catalog:",
    "drizzle-kit": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  }
}
