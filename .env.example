# OAuth apps.
# GitHub OAuth Apps have a single callback URL, so create dev/prod apps:
#   tokenmaxxing-dev -> http://api.tokenmaxxing.localhost:8788/auth/github/callback
#   tokenmaxxing     -> https://api.tokenmaxxing.sh/auth/github/callback
# Google Web OAuth clients should include:
#   http://api.tokenmaxxing.localhost:8788/auth/google/callback
#   https://api.tokenmaxxing.sh/auth/google/callback
# This file (.env) holds DEV pairs and only feeds `bun run dev`.
# Deploys load .env.production (prod pairs) via --env-file;
# CI deploys on main pushes use repo secrets instead.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# Comma-separated GitHub logins for members whose Discord account is linked.
DISCORD_GITHUB_LOGINS=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
